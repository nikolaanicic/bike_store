CREATE TABLE IF NOT EXISTS Users (
  CITIZEN_ID VARCHAR(13) NOT NULL PRIMARY KEY,
  NAME VARCHAR(50) NOT NULL,
  LAST_NAME VARCHAR(50) NOT NULL,
  ADDRESS VARCHAR(100) NOT NULL,
  ACTIVE_BIKES INTEGER NOT NULL DEFAULT 0
);


