CREATE TABLE IF NOT EXISTS Bikes (
  ID INTEGER NOT NULL AUTO_INCREMENT PRIMARY KEY,
  TYPE ENUM('MOUNTAIN', 'CITY', 'ROAD') NOT NULL,
  AVAILABLE INT NOT NULL DEFAULT 0,
  CITY ENUM('KRAGUJEVAC', 'NOVISAD', 'SUBOTICA') NOT NULL
);
